// put prototypes for public functions, explain what it does
// put your names here, date
//these functions are included so the header files in the SpaceInvaders.c will have access to the ADC functions
void ADC_Init(void);
uint64_t ADC_In(void);










